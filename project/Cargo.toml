[package]
name = "project"
version = "0.1.0"
edition = "2024"

[lib]
name = "project"
path = "src/lib.rs"
crate-type = ["dylib"]

[dependencies]
tokio = { version = "1.48.0", features = ["macros", "rt-multi-thread", "sync"] }
